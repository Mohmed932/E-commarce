/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['eg.jumia.is','townteam.com',"1000logos.net","upload.wikimedia.org"],
    },
};

export default nextConfig;
